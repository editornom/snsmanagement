---
scope: SNS 콘텐츠 제작 자동화 도구 - 전체 아키텍처(PRD 1건, 에픽 분할 전)
purpose: 1인 개발자(Junghoo)가 빌드 서브스트레이트로 쓸 결정 스펙. 산출물은 스파인 단독
altitude: initiative
updated: 2026-06-25T13:20
---

- (decision) 스택 확정: Electron + TypeScript. Main 프로세스가 파일시스템/Claude API/HTML-PNG 렌더링/ffmpeg 영상조립 전담, Renderer는 웹뷰 UI+편집기만(contenteditable+코드창). IPC로 통신, contextBridge로 격리
- (decision) 상태저장: DB 없음, 파일시스템(output/ 폴더)이 단일 진실 소스. 음악 로테이션 마지막 사용곡 등은 앱 설정폴더에 작은 JSON 상태파일로 추적
- (decision) Claude API 키: Electron safeStorage(OS 암호화, Windows DPAPI)로 앱 사용자데이터 폴더에 저장. 멀티PC 동기화 없음 - PC 바꿀때 설정화면에서 재입력(1인 도구라 과설계 배제)
- (decision) 정보카드 HTML 생성: 절충 방식 확정. 골격(제목바/불릿박스/아이콘/푸터 영역, 클릭동기화용 data-edit-id 표식, 색상 CSS변수 네이밍)은 고정 계약(contract)으로 강제, 그 안의 실제 비주얼(아이콘모양/불릿스타일/배치)은 Claude가 참고이미지 기반으로 자유 생성. 시스템 프롬프트로 계약 준수 강제
- (decision) 카드 전환 애니메이션(FR-5/FR-6): 실제 CSS 애니메이션을 녹화하는 대신, 정적 카드 PNG들을 ffmpeg의 내장 xfade 필터(fade/slide 계열)로 이어붙여 전환효과 구현. 프레임캡처 파이프라인 불필요, 타이밍 정확도 ffmpeg가 보장
- (decision) HTML-PNG 렌더링: Electron 자체 offscreen BrowserWindow + capturePage 사용(Playwright 등 별도 Chromium 의존성 추가 안함, 에디터 미리보기와 동일 엔진이라 렌더 결과 100% 일치 보장)
- (decision) 영상 조립: ffmpeg-static(npm, 사전빌드 바이너리 동봉)으로 시스템에 ffmpeg 별도설치 요구 안함. Claude API 호출은 공식 @anthropic-ai/sdk(Node)로 main 프로세스에서만 수행
- (decision) 배포/운영: 자동업데이트 없음. electron-builder NSIS 설치파일 수동 재설치 방식. 별도 배포서버/스테이징 환경 없음(로컬 단일 환경). 에러는 인앱 다이얼로그+로컬 로그파일로만 노출(원격 모니터링/텔레메트리 없음, 1인 도구)
- (event) spine finalized (lint clean, reviewer gate skipped - hobby/solo stakes)
- (decision) 구조시드 보완: output/{키워드}/{YYMMDD}/meta.json 추가 - 키워드/제목/홈페이지URL 저장(썸네일 등록시 입력), FR-7 원고생성 단계가 이 파일을 읽어 URL 자동삽입. PRD FR-7/FR-8 변경(URL 입력시점 이동)에 따른 보완
- (decision) FR-9(자연어 AI편집) 추가에 따른 보완: AD-2 Binds에 FR-9 추가, Capability Map에 FR-9 행 추가. main/api/ 클라이언트는 FR-1과 동일 재사용, 별도 인증경로 없음(Claude Code/Pro 연동 기각)
- (decision) AD-4 정정(사용자 지적): ffmpeg xfade 정적전환 방식 폐기. 실제로는 썸네일만 단일 PNG, 이후 카드들은 HTML/CSS 애니메이션이 실제 재생되는 걸 offscreen BrowserWindow에서 고정FPS로 capturePage 반복호출해 프레임캡처. ffmpeg는 프레임시퀀스+오디오 조립에만 사용. 카드간 전환은 다음카드 자체 진입애니메이션이 담당, 별도 xfade 없음
- (decision) AD-4 재정정: 정지노출구간 캡처생략 최적화 철회(사용자 지적- 반복루핑 모션그래픽 가능성 있어 정지로 가정 불가). 노출시간 전체(진입애니메이션+추가노출3초)를 60fps로 빠짐없이 캡처. 프레임은 인코딩후 임시파일 삭제, 파이프라인은 API 미호출
- (decision) 참고파일(utm-slides.html) 검토 결과: 사용자가 단순 텍스트 페이드+슬라이드 전환 수준으로 범위 확정. JS setTimeout 오케스트레이션/SVG 드로잉 등 복잡한 모션그래픽은 불필요 - CDP virtual time policy 같은 장치 추가 안 함. AD-4는 순수 CSS keyframe 타임라인 프레임스테핑만으로 충분, 변경 없음
- (version) 스택 보완(Story1.1 작성중 웹리서치): electron-vite latest 추가 - 2026년 기준 공식 권장 빌드도구, main/preload/renderer 분리가 기존 파라다임과 일치. electron-builder는 패키징 단계에서 그대로 유지
- (decision) Story1.1 작성중 결정: 렌더러 프레임워크 React+TypeScript 확정(electron-vite react-ts 템플릿). 스파인엔 미명시였던 항목 - 등록폼/에디터/코드패널/설정화면 등 다수 UI 화면 구현 편의 목적. CodeMirror/Monaco는 Story1.5에서 별도 검토
